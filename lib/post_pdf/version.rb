# frozen_string_literal: true

module PostPdf
  # Bump when templates/CSS semantics change so digests invalidate cleanly.
  TEMPLATE_VERSION = "7"
  VERSION = "0.1.0"
  THEMES = %w[light dark].freeze
end
